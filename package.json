{
  "name": "bun",
  "version": "1.4.0",
  "description": "Incredibly fast JavaScript runtime, bundler, test runner, and package manager – all in one",
  "private": true,
  "scripts": {
    "mcp:postgres": "npx -y @modelcontextprotocol/server-postgres postgresql://localhost:5432/bun_dev"
  },
  "devDependencies": {
    "@modelcontextprotocol/server-postgres": "^0.6.2"
  }
}
