{
  "name": "bun-dev-toolchain",
  "version": "1.0.0",
  "description": "Dev toolchain configuration for Bun project with PostgreSQL MCP querying",
  "devDependencies": {
    "@modelcontextprotocol/server-postgres": "^0.6.2"
  }
}